import { SinglePartner } from "@/components/ui/SinglePartner/SinglePartner";

import { autoPartnerData } from "./AutoPartner.data";

export function AutoPartner() {
  return <SinglePartner {...autoPartnerData} />;
}
